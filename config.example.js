// Copy this file to config.js and fill in your Supabase project's values
// (Project Settings → API in the Supabase dashboard).
// config.js is gitignored — each environment (your machine, production) has
// its own, pointing at its own Supabase project. Never commit real values.
window.SUPABASE_CONFIG = {
  url: 'https://YOUR_PROJECT.supabase.co',
  anonKey: 'YOUR_ANON_KEY',
};
